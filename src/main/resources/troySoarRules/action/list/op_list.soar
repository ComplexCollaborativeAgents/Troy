##!
# @file
#
# @created smohan 20170413

sp {action*apply*op_list
    (state <s> ^name action
               ^operator <op>
               ^io.output-link <ol>)
    (<op> ^name op_list
          ^argument <arg>)
-->
    (<ol> ^send-message <sm>)
    (<sm> ^dialog-act inform
          ^predicate exists
          ^objects <arg>)
    }