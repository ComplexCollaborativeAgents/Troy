##!
# @file
#
# @created smohan 20170810

sp {task*elaborate*name
	(state <s>	^superstate <ss>
            	^problemspace <ps>)
    (<ss>   ^indexed-action <ia>
            ^operator <ia>)
-->
	(<s>	^name task)
 }

sp {task*elaborate*problemspace*if-desired
	(state <s>	^name task
            	^desired <d>
             	^problemspace <ps>)
-->
	(<ps>	^action-info actionable)
 }

sp {task*elaborate*task-segment-id
	(state <s>	^name task
            	^interaction.stack.segment._metadata.id <id>)
-->
	(<s>	^task-segment-id <id>)
 }

sp {task*elaborate*subtask-segment-id
	(state <s>	^name task
            	^task-segment-id <id>)
-->
	(<s>	^subtask-segment-id (* 100 <id>))
 }

#sp {task*elaborate*desired
#	(state <s>	^name task
#            	^superstate.desired <d>)
#-->
#	(<s>	^desired <d>)
# }

sp {task*write*indicate-successful-execution
	(state <s>	^name task
            	^desired <d>
             	^success <d>
              	^superstate <ss>)
-->
	(<ss> ^success <d>)
  }